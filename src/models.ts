export type {
  User,
  Vote,
  Name,
  Users,
  Names,
  Data,
  SetNewValue,
  SetNewName,
  SetNewUser,
} from "./hooks/useDatabase";

export type { LoginAction, LogoutAction } from "./hooks/useAuth";
